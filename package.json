{
  "name": "contact-manager-fullstack",
  "version": "1.0.0",
  "description": "Full-stack Contact Manager with React + Node.js + MongoDB",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "echo 'No build needed for backend'",
    "start": "cd server && npm install --production && node server.js",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "lint": "npm run lint --prefix client",
    "test": "npm run test --prefix server"
  },
  "keywords": ["react", "nodejs", "mongodb", "redux", "express", "fullstack"],
  "author": "Daria Tkachenko",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}